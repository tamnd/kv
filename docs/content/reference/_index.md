---
title: "Reference"
linkTitle: "Reference"
description: "The complete surface of kv: the library API, every CLI command and flag, every configuration option and pragma, and the on-disk layout."
weight: 30
---

The guides teach tasks; this section is the exhaustive list. The [library reference](/reference/library/) covers every exported type, method, option, and error. The [CLI reference](/reference/cli/) covers every command and flag. The [configuration reference](/reference/configuration/) covers every open option and pragma with its default and range, plus the files kv writes on disk.
