---
title: "Reference"
linkTitle: "Reference"
description: "The complete surface of kv: the library API, every server flag and command, every configuration option, and the on-disk layout."
weight: 30
---

The guides teach tasks; this section is the exhaustive list.
The [library reference](/reference/library/) covers every exported type, method, option, and return.
The [server reference](/reference/server/) covers every flag and supported RESP command.
The [configuration reference](/reference/configuration/) covers every option with its default and effect, plus the files kv writes on disk.
