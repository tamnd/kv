title       = "kv"
baseURL     = "https://kv.tamnd.com/"
description = "kv is an embeddable key/value database for Go. One file on disk, zero external dependencies, full ACID transactions, and a sharded hash-log storage core that keeps point lookups flat as the database grows and runs over datasets larger than memory. It feels like SQLite, for keys and values."
contentDir  = "content"
outputDir   = "public"
theme       = "tago-doks"
syntaxHighlight = "true"

[params]
github = "https://github.com/tamnd/kv"
